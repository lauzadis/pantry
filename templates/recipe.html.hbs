<html>
    <head>
        <meta charset="utf-8"/>
    </head>
    {{> frameworks }}
    <body>
        {{> navbar }}
        <table>
            <tr>
                <th>ID</th>
                <th>Name</th>
            </tr>
            {{#each recipes}}
            <tr>
                <td data="id">{{_id.$oid}}</td>
                <td data="name">{{name}}</td>
            </tr>
            {{/each}}
        </table>
    </body>
    
</html>
